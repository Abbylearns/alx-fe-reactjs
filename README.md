# Tailwind React Integration

This project demonstrates how to set up and integrate **Tailwind CSS** into a **React application using Vite**.

## Setup Instructions

1. **Create a React project with Vite**
   ```bash
   npm create vite@latest tailwind-react-integration -- --template react
   cd tailwind-react-integration
